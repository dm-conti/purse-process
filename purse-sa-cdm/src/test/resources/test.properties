# Define the address of the broker in URL format.
# broker.url=tcp://127.0.0.1:61616
broker.url=tcp://127.0.0.1:61616
# Define the file where to read the serviceTranslator configuration.
serviceTranslator.configuration=entity.xml